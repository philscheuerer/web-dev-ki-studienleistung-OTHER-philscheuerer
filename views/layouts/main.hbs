<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    <meta charset="UTF-8">

    <style>
        .nav-item a {
            color: black;
            text-decoration: none;
        }
    </style>
    <!-- Should be included via styles.css but does not work. -->

    <!-- house icon for nav-->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
</head>
<body class="p-4 m-3">
<section class="container">
    <div class="row">
        <div class="col-md-3">
            <img src="/img/logo.svg" alt="Logo OTHER" class="img-fluid p-1 m-1" style="width: 100px; height: 50px;">

            <!-- vertical navigation -->
            <ul class="nav flex-column">
            {{#unless isIndexPage}}
                    <li class="nav-item">
                        <div class="fs-3">
                            <i class="bi bi-house-door-fill"></i>
                            <a class="align-middle" href="/dashboard">Startseite</a>
                        </div>
                    </li>
                {{/unless}}
                {{#if isIndexPage}}
                    <li class="nav-item">
                        <div class="fs-3">
                            <i class="bi bi-house-door-fill"></i>
                            <a class="align-middle" href="/">Startseite</a>
                        </div>
                    </li>
                    <li class="nav-item">
                        <div class="fs-3">
                            <a href="/login" class="btn btn-primary rounded-pill w-100">Login</a>
                        </div>
                    </li>
                    <li class="nav-item">
                        <div class="fs-3">
                            <a href="/signup" class="btn btn-primary rounded-pill w-100">Registrieren</a>
                        </div>
                    </li>
                {{else}}
                    <li class="nav-item">
                        <div class="fs-3">
                            <i class="bi bi-people-fill"></i>
                            <a class="align-middle" href="/users">Nutzer</a>
                        </div>
                    </li>
                    <li class="nav-item">
                        <div class="fs-3">
                            <i class="bi bi-person-fill"></i>
                            <a class="align-middle" href="/profile">Profil</a>
                        </div>
                    </li>
                    <li class="nav-item">
                        <div class="fs-3">
                            <i class="bi bi-controller"></i>
                            <a class="align-middle" href="/game">Game</a>
                        </div>
                    </li>
                {{/if}}
                {{#unless isIndexPage}}
                        <li class="nav-item">
                            <div class="fs-3">
                                <a href="/othern" class="btn btn-primary rounded-pill w-100">Othern</a>
                            </div>
                        </li>
                        <li class="nav-item">
                            <div class="fs-3">
                                <a href="/" class="btn btn-primary rounded-pill w-100">Logout</a>
                            </div>
                        </li>
                {{/unless}}
            </ul>
        </div>
        <div class="col-md-6">
            {{{body}}}
        </div>

        <div class="col-md-3">
            {{#unless isIndexPage}}
                <form action="/search" method="post">
                    <div class="input-group">
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-search"></i>
                        </button>
                        <input type="text" name="searchTerm" class="form-control" placeholder="Other durchsuchen">
                    </div>
                </form>
            {{/unless}}
            {{>trends}}
        </div>

    </div>
</section>
</body>
</html>
